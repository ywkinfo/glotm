# Legacy Readers Archive

이 디렉터리는 루트 `GloTm` 셸로 통합되기 전의 독립 `LatTm` / `MexTm` SPA 런타임 보관본입니다.

- 현재 활성 런타임은 루트 `src/app/App.tsx`와 `src/products/*`입니다.
- 현재 운영 상태와 우선순위는 루트 `PROJECT-OVERVIEW.md`를, 런타임 메타데이터는 `src/products/registry.ts`를, 실행 명령은 `README.md`를 기준으로 확인합니다.
- `LatTm/`와 `MexTm/`에는 콘텐츠 원고, 빌드 스크립트, 생성 산출물, 일부 공유 자산만 남깁니다.
- 필요 시 과거 독립 리더 구조를 참조하거나 복구할 수 있도록 원래 패키지별 하위 구조를 최대한 유지해 이동했습니다.
